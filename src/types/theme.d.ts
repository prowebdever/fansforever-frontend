export interface ThemeObj {
  accentColors: {
    primary: string;
  };
  backgroundColors: {
    primary: string;
    secondary: string;
  };
  textColors: {
    primary: string;
    secondary: string;
    tertiary: string;
  };
  borderColors: {
    tertiary: string;
  };
}
